name: Super Secret Deploy Shenanigans

on: push

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Unleash the Kraken
        run: echo "Nothing to see here, just a wild goose chase! 🦆"